{"attempt_id": 1, "candidate_commit": "88a6a7fd9e9a1df309fe320852a36a04073f8841", "decision": "accepted", "diagnosis": {"reason": "Replaces pure random search with a hybrid: continued global exploration early on plus Gaussian local refinement with annealed step size around the incumbent best. This preserves global coverage while converging tightly into basins, so run_search() finds the global minimum far more reliably than uniform random sampling."}, "diff": {"integrity": "ok", "path": ".nanorsi/runs/19b2dc7337/proposal/proposal.diff", "sha256": "d61646dcd089bec5f140a5808190f2cf8316445f7af5343343483c16b23386b5", "stats": {"target/program.py": [19, 6]}, "text": "diff --git a/target/program.py b/target/program.py\n--- a/target/program.py\n+++ b/target/program.py\n@@ -10,28 +10,37 @@\n def search_algorithm(iterations=1000, bounds=(-5, 5)):\n     \"\"\"\n-    A simple random search algorithm that often gets stuck in local minima.\n+    Hybrid global random search with adaptive local refinement.\n \n     Args:\n         iterations: Number of iterations to run\n         bounds: Bounds for the search space (min, max)\n \n     Returns:\n         Tuple of (best_x, best_y, best_value)\n     \"\"\"\n-    # Initialize with a random point\n+    lo, hi = bounds\n+    width = hi - lo\n+\n+    # Initialize with a random point\n     best_x = np.random.uniform(bounds[0], bounds[1])\n     best_y = np.random.uniform(bounds[0], bounds[1])\n     best_value = evaluate_function(best_x, best_y)\n \n-    for _ in range(iterations):\n-        # Simple random search\n-        x = np.random.uniform(bounds[0], bounds[1])\n-        y = np.random.uniform(bounds[0], bounds[1])\n+    for i in range(iterations):\n+        # Anneal: mix global exploration with shrinking local refinement\n+        progress = i / max(1, iterations - 1)\n+        if np.random.rand() < 0.3 * (1.0 - progress) + 0.05:\n+            # Global exploration\n+            x = np.random.uniform(lo, hi)\n+            y = np.random.uniform(lo, hi)\n+        else:\n+            # Local Gaussian perturbation around the best point\n+            sigma = 0.5 * width * (1.0 - progress) + 1e-3\n+            x = best_x + np.random.normal(0.0, sigma)\n+            y = best_y + np.random.normal(0.0, sigma)\n+            x = min(max(x, lo), hi)\n+            y = min(max(y, lo), hi)\n         value = evaluate_function(x, y)\n \n         if value < best_value:\n             best_value = value\n             best_x, best_y = x, y\n"}, "evaluator": {"candidate_cases": [{"group_id": "fnmin-validation", "repeat_id": 0, "score": 0.9961592325369022, "status": "ok", "task_id": "fnmin-validation"}], "parent_cases": [{"group_id": "fnmin-validation", "repeat_id": 0, "score": 0.9193876162472744, "status": "ok", "task_id": "fnmin-validation"}]}, "generation": 1, "metric": {"candidate": 0.9961592325369022, "delta": 0.07677161628962781, "direction": "maximize", "name": "score", "parent": 0.9193876162472744}, "proposer_harness_commit": "824df113c62e6bc1e20a35ea852e3fd858245ce7", "reason": "improvement 0.076772", "schema_version": 1, "surface": ["target/program.py"]}
{"attempt_id": 2, "candidate_commit": null, "decision": "failed", "diagnosis": null, "diff": {"integrity": "ok", "path": ".nanorsi/runs/331110d160/proposal/proposal.diff", "sha256": "63b210a8b12c554466114f0be824b2b8c6661c0591a51ae9b21116e438e13388", "stats": {"target/program.py": [66, 39]}, "text": "diff --git a/target/program.py b/target/program.py\nindex 1111111..2222222 100644\n--- a/target/program.py\n+++ b/target/program.py\n@@ -1,54 +1,79 @@\n \"\"\"Function minimization example for OpenEvolve\"\"\"\n import numpy as np\n \n \n def search_algorithm(iterations=1000, bounds=(-5, 5)):\n-    \"\"\"\n-    Hybrid global random search with adaptive local refinement.\n-\n-    Args:\n-        iterations: Number of iterations to run\n-        bounds: Bounds for the search space (min, max)\n-\n-    Returns:\n-        Tuple of (best_x, best_y, best_value)\n-    \"\"\"\n-    lo, hi = bounds\n-    width = hi - lo\n-\n-    # Initialize with a random point\n-    best_x = np.random.uniform(bounds[0], bounds[1])\n-    best_y = np.random.uniform(bounds[0], bounds[1])\n-    best_value = evaluate_function(best_x, best_y)\n-\n-    for i in range(iterations):\n-        # Anneal: mix global exploration with shrinking local refinement\n-        progress = i / max(1, iterations - 1)\n-        if np.random.rand() < 0.3 * (1.0 - progress) + 0.05:\n-            # Global exploration\n-            x = np.random.uniform(lo, hi)\n-            y = np.random.uniform(lo, hi)\n-        else:\n-            # Local Gaussian perturbation around the best point\n-            sigma = 0.5 * width * (1.0 - progress) + 1e-3\n-            x = best_x + np.random.normal(0.0, sigma)\n-            y = best_y + np.random.normal(0.0, sigma)\n-            x = min(max(x, lo), hi)\n-            y = min(max(y, lo), hi)\n-        value = evaluate_function(x, y)\n-\n-        if value < best_value:\n-            best_value = value\n-            best_x, best_y = x, y\n-\n-    return best_x, best_y, best_value\n+    \"\"\"\n+    Multi-restart hybrid search: a space-filling global phase followed by\n+    independent annealed local refinements of the most promising basins.\n+\n+    Args:\n+        iterations: Number of iterations to run\n+        bounds: Bounds for the search space (min, max)\n+\n+    Returns:\n+        Tuple of (best_x, best_y, best_value)\n+    \"\"\"\n+    lo, hi = bounds\n+    width = hi - lo\n+\n+    def clamp(v):\n+        return min(max(v, lo), hi)\n+\n+    def local_refine(x0, y0, budget):\n+        \"\"\"Annealed Gaussian local search around a seed point.\"\"\"\n+        bx, by = x0, y0\n+        bval = evaluate_function(bx, by)\n+        for i in range(budget):\n+            progress = i / max(1, budget - 1)\n+            # Geometric schedule: fast early, very fine near the end\n+            sigma = 0.2 * width * (0.05 ** progress) + 1e-6\n+            # Occasionally take a larger step to escape small ripples\n+            if np.random.rand() < 0.1:\n+                sigma *= 5.0\n+            x = clamp(bx + np.random.normal(0.0, sigma))\n+            y = clamp(by + np.random.normal(0.0, sigma))\n+            value = evaluate_function(x, y)\n+            if value < bval:\n+                bval = value\n+                bx, by = x, y\n+        return bx, by, bval\n+\n+    # Phase 1: space-filling global sampling (Sobol-like low discrepancy via\n+    # stratified LHS) to cover the domain without relying on luck.\n+    n_global = max(32, int(iterations * 0.25))\n+    samples = []\n+    per_side = max(1, int(np.ceil(np.sqrt(n_global))))\n+    cells = [(i, j) for i in range(per_side) for j in range(per_side)]\n+    np.random.shuffle(cells)\n+    for (ci, cj) in cells[:n_global]:\n+        x = lo + width * ((ci + np.random.rand()) / per_side)\n+        y = lo + width * ((cj + np.random.rand()) / per_side)\n+        samples.append((evaluate_function(x, y), x, y))\n+    samples.sort()\n+\n+    # Phase 2: refine the top basins independently so a single unlucky\n+    # restart cannot dominate the outcome.\n+    n_refine = max(1, int(iterations * 0.75))\n+    n_basins = min(5, len(samples))\n+    per_basin = max(1, n_refine // n_basins)\n+\n+    best_x, best_y, best_value = local_refine(samples[0][1], samples[0][2], n_refine)\n+    for k in range(1, n_basins):\n+        _, sx, sy = samples[k]\n+        # Keep basins that are distinct enough to be genuinely different\n+        if min((sx - best_x) ** 2 + (sy - best_y) ** 2 for _ in [0]) < (width / n_basins) ** 2 and k > 1:\n+            pass\n+        x, y, v = local_refine(sx, sy, per_basin)\n+        if v < best_value:\n+            best_x, best_y, best_value = x, y, v\n+\n+    return best_x, best_y, best_value\n "}, "evaluator": {"candidate_cases": null, "parent_cases": null}, "generation": null, "metric": {"candidate": null, "delta": null, "direction": "maximize", "name": "score", "parent": null}, "proposer_harness_commit": null, "reason": "error: corrupt patch at line 116\n", "schema_version": 1, "surface": null}
{"attempt_id": 3, "candidate_commit": "4077543ec0ffdca3e1ecf552e0dade6d8af32b66", "decision": "rejected", "diagnosis": {"reason": "Replace a single-point random walk with a quasi-random (Halton) global scan that covers the whole domain deterministically, then annealed local refinement from the top candidates. This reliably lands in the global basin and hones in with geometrically shrinking steps, so run_search() finds the global minimum with much higher consistency than the previous 30/70 mixed random sampler, which could miss the basin entirely or converge slowly."}, "diff": {"integrity": "ok", "path": ".nanorsi/runs/e5484ef57e/proposal/proposal.diff", "sha256": "b9e058706baa83d5f5e05109ec383f96bbbd30f3c0aa4b33e899c7b40b0b8f8c", "stats": {"target/program.py": [65, 39]}, "text": "diff --git a/target/program.py b/target/program.py\nindex 0000000..0000001\n--- a/target/program.py\n+++ b/target/program.py\n@@ -8,48 +8,80 @@ import numpy as np\n \n \n def search_algorithm(iterations=1000, bounds=(-5, 5)):\n-    \"\"\"\n-    Hybrid global random search with adaptive local refinement.\n-\n-    Args:\n-        iterations: Number of iterations to run\n-        bounds: Bounds for the search space (min, max)\n-\n-    Returns:\n-        Tuple of (best_x, best_y, best_value)\n-    \"\"\"\n-    lo, hi = bounds\n-    width = hi - lo\n-\n-    # Initialize with a random point\n-    best_x = np.random.uniform(bounds[0], bounds[1])\n-    best_y = np.random.uniform(bounds[0], bounds[1])\n-    best_value = evaluate_function(best_x, best_y)\n-\n-    for i in range(iterations):\n-        # Anneal: mix global exploration with shrinking local refinement\n-        progress = i / max(1, iterations - 1)\n-        if np.random.rand() < 0.3 * (1.0 - progress) + 0.05:\n-            # Global exploration\n-            x = np.random.uniform(lo, hi)\n-            y = np.random.uniform(lo, hi)\n-        else:\n-            # Local Gaussian perturbation around the best point\n-            sigma = 0.5 * width * (1.0 - progress) + 1e-3\n-            x = best_x + np.random.normal(0.0, sigma)\n-            y = best_y + np.random.normal(0.0, sigma)\n-            x = min(max(x, lo), hi)\n-            y = min(max(y, lo), hi)\n-        value = evaluate_function(x, y)\n-\n-        if value < best_value:\n-            best_value = value\n-            best_x, best_y = x, y\n-\n-    return best_x, best_y, best_value\n+    \"\"\"\n+    Two-phase search: coarse low-discrepancy global scan to find promising\n+    basins, then iterative local refinement around the best candidates with\n+    an annealed step size.\n+\n+    Args:\n+        iterations: Number of iterations to run\n+        bounds: Bounds for the search space (min, max)\n+\n+    Returns:\n+        Tuple of (best_x, best_y, best_value)\n+    \"\"\"\n+    lo, hi = bounds\n+    width = hi - lo\n+\n+    best_x = np.random.uniform(lo, hi)\n+    best_y = np.random.uniform(lo, hi)\n+    best_value = evaluate_function(best_x, best_y)\n+\n+    # Phase 1: quasi-random (Halton-like) global scan, ~30% of budget.\n+    n_scan = max(20, int(0.3 * iterations))\n+\n+    def _halton(index, base):\n+        f = 1.0\n+        r = 0.0\n+        while index > 0:\n+            f /= base\n+            r += f * (index % base)\n+            index //= base\n+        return r\n+\n+    candidates = []\n+    for i in range(1, n_scan + 1):\n+        x = lo + width * _halton(i, 2)\n+        y = lo + width * _halton(i, 3)\n+        v = evaluate_function(x, y)\n+        candidates.append((v, x, y))\n+        if v < best_value:\n+            best_value, best_x, best_y = v, x, y\n+\n+    # Keep several promising seeds for local refinement.\n+    candidates.sort()\n+    seeds = [(x, y) for _, x, y in candidates[: max(1, len(candidates) // 10)]]\n+    seeds.append((best_x, best_y))\n+\n+    # Phase 2: annealed local refinement from the best seed, plus occasional\n+    # restarts from other seeds to escape shallow basins.\n+    sigma = 0.1 * width\n+    min_sigma = 1e-6 * width\n+    i = 0\n+    while i < iterations - n_scan:\n+        for sx, sy in seeds:\n+            if i >= iterations - n_scan:\n+                break\n+            step = max(sigma, min_sigma)\n+            x = min(max(sx + np.random.normal(0.0, step), lo), hi)\n+            y = min(max(sy + np.random.normal(0.0, step), lo), hi)\n+            v = evaluate_function(x, y)\n+            i += 1\n+            if v < best_value:\n+                best_value, best_x, best_y = v, x, y\n+        # Shrink the step size gradually each sweep.\n+        sigma *= 0.8\n+\n+    return best_x, best_y, best_value\n \n \n # EVOLVE-BLOCK-END\n"}, "evaluator": {"candidate_cases": [{"group_id": "fnmin-validation", "repeat_id": 0, "score": 0.9495968124355065, "status": "ok", "task_id": "fnmin-validation"}], "parent_cases": [{"group_id": "fnmin-validation", "repeat_id": 0, "score": 0.9961592325369022, "status": "ok", "task_id": "fnmin-validation"}]}, "generation": 1, "metric": {"candidate": 0.9495968124355065, "delta": -0.04656242010139566, "direction": "maximize", "name": "score", "parent": 0.9961592325369022}, "proposer_harness_commit": "824df113c62e6bc1e20a35ea852e3fd858245ce7", "reason": "improvement below minimum: -0.046562", "schema_version": 1, "surface": ["target/program.py"]}
{"attempt_id": 4, "candidate_commit": "7da7b13c996aa725601a2c73b26b702849ea8cd5", "decision": "rejected", "diagnosis": {"reason": "Dedicating 40% of evaluations to uniform global sampling with a top-k seed pool reduces the chance of missing the global basin; annealed Gaussian refinement around the best seed (with occasional jumps to alternate seeds) then converges tightly, and a final small-sigma polish sharpens the minimum, improving value/reliability scores over the single-seed baseline."}, "diff": {"integrity": "ok", "path": ".nanorsi/runs/a332530ffe/proposal/proposal.diff", "sha256": "6aa2a40e59f9a8d133c9c21a435c36d2e6ed36232ceca9f0be9840dff36bf4da", "stats": {"target/program.py": [50, 15]}, "text": "diff --git a/target/program.py b/target/program.py\nindex 0000000..0000001 100644\n--- a/target/program.py\n+++ b/target/program.py\n@@ -10,44 +10,65 @@\n def search_algorithm(iterations=1000, bounds=(-5, 5)):\n     \"\"\"\n-    Hybrid global random search with adaptive local refinement.\n+    Multi-start global random search with shrinking local refinement\n+    around the best candidates.\n \n     Args:\n         iterations: Number of iterations to run\n         bounds: Bounds for the search space (min, max)\n \n     Returns:\n         Tuple of (best_x, best_y, best_value)\n     \"\"\"\n     lo, hi = bounds\n     width = hi - lo\n \n-    # Initialize with a random point\n-    best_x = np.random.uniform(bounds[0], bounds[1])\n-    best_y = np.random.uniform(bounds[0], bounds[1])\n-    best_value = evaluate_function(best_x, best_y)\n+    # Phase 1: spend part of the budget on uniform global sampling,\n+    # keeping the top-k candidates as seeds for local refinement.\n+    n_global = max(1, int(iterations * 0.4))\n+    k = 5\n+    # Small candidate pool: (value, x, y)\n+    pool = []\n \n+    for i in range(n_global):\n+        x = np.random.uniform(lo, hi)\n+        y = np.random.uniform(lo, hi)\n+        value = evaluate_function(x, y)\n+        if len(pool) < k:\n+            pool.append((value, x, y))\n+            pool.sort(key=lambda t: t[0])\n+        elif value < pool[-1][0]:\n+            pool[-1] = (value, x, y)\n+            pool.sort(key=lambda t: t[0])\n+\n+    best_value, best_x, best_y = pool[0]\n+\n+    # Phase 2: annealed local refinement around the best seed,\n+    # with periodic restarts from other seeds and a final polish.\n     for i in range(iterations):\n-        # Anneal: mix global exploration with shrinking local refinement\n-        progress = i / max(1, iterations - 1)\n-        if np.random.rand() < 0.3 * (1.0 - progress) + 0.05:\n-            # Global exploration\n-            x = np.random.uniform(lo, hi)\n-            y = np.random.uniform(lo, hi)\n+        it = i + n_global\n+        progress = it / max(1, iterations - 1)\n+        r = np.random.rand()\n+        if r < 0.05:\n+            # Occasional late global exploration\n+            x = np.random.uniform(lo, hi)\n+            y = np.random.uniform(lo, hi)\n         else:\n-            # Local Gaussian perturbation around the best point\n-            sigma = 0.5 * width * (1.0 - progress) + 1e-3\n-            x = best_x + np.random.normal(0.0, sigma)\n-            y = best_y + np.random.normal(0.0, sigma)\n+            if r < 0.15 and len(pool) > 1:\n+                # Jump to a random alternative seed to escape bad basins\n+                _, sx, sy = pool[np.random.randint(1, len(pool))]\n+            else:\n+                sx, sy = best_x, best_y\n+            sigma = 0.3 * width * (1.0 - progress) ** 2 + 1e-4\n+            x = sx + np.random.normal(0.0, sigma)\n+            y = sy + np.random.normal(0.0, sigma)\n             x = min(max(x, lo), hi)\n             y = min(max(y, lo), hi)\n         value = evaluate_function(x, y)\n \n         if value < best_value:\n             best_value = value\n             best_x, best_y = x, y\n+            pool[0] = (best_value, best_x, best_y)\n+\n+    # Final fine-coordinate polish around the best point\n+    for _ in range(200):\n+        x = best_x + np.random.normal(0.0, 1e-3)\n+        y = best_y + np.random.normal(0.0, 1e-3)\n+        x = min(max(x, lo), hi)\n+        y = min(max(y, lo), hi)\n+        value = evaluate_function(x, y)\n+        if value < best_value:\n+            best_value = value\n+            best_x, best_y = x, y\n \n     return best_x, best_y, best_value\n"}, "evaluator": {"candidate_cases": [{"group_id": "fnmin-validation", "repeat_id": 0, "score": 0.9093517594510742, "status": "ok", "task_id": "fnmin-validation"}], "parent_cases": [{"group_id": "fnmin-validation", "repeat_id": 0, "score": 0.9961592325369022, "status": "ok", "task_id": "fnmin-validation"}]}, "generation": 1, "metric": {"candidate": 0.9093517594510742, "delta": -0.08680747308582804, "direction": "maximize", "name": "score", "parent": 0.9961592325369022}, "proposer_harness_commit": "824df113c62e6bc1e20a35ea852e3fd858245ce7", "reason": "improvement below minimum: -0.086807", "schema_version": 1, "surface": ["target/program.py"]}
{"attempt_id": 5, "candidate_commit": null, "decision": "failed", "diagnosis": null, "diff": {"integrity": "ok", "path": ".nanorsi/runs/cf52e51c52/proposal/proposal.diff", "sha256": "5daabd42632d08f75e0bab8db90ce6ae7b058625d451d1c19ab36f7a246f3a1e", "stats": {"target/program.py": [44, 29]}, "text": "diff --git a/target/program.py b/target/program.py\n--- a/target/program.py\n+++ b/target/program.py\n@@ -11,46 +11,46 @@ import numpy as np\n def search_algorithm(iterations=1000, bounds=(-5, 5)):\n@@\n-    lo, hi = bounds\n-    width = hi - lo\n-\n-    # Initialize with a random point\n-    best_x = np.random.uniform(bounds[0], bounds[1])\n-    best_y = np.random.uniform(bounds[0], bounds[1])\n-    best_value = evaluate_function(best_x, best_y)\n-\n-    for i in range(iterations):\n-        # Anneal: mix global exploration with shrinking local refinement\n-        progress = i / max(1, iterations - 1)\n-        if np.random.rand() < 0.3 * (1.0 - progress) + 0.05:\n-            # Global exploration\n-            x = np.random.uniform(lo, hi)\n-            y = np.random.uniform(lo, hi)\n-        else:\n-            # Local Gaussian perturbation around the best point\n-            sigma = 0.5 * width * (1.0 - progress) + 1e-3\n-            x = best_x + np.random.normal(0.0, sigma)\n-            y = best_y + np.random.normal(0.0, sigma)\n-            x = min(max(x, lo), hi)\n-            y = min(max(y, lo), hi)\n-        value = evaluate_function(x, y)\n-\n-        if value < best_value:\n-            best_value = value\n-            best_x, best_y = x, y\n-\n-    return best_x, best_y, best_value\n+    lo, hi = bounds\n+    width = hi - lo\n+\n+    # Phase 1: quasi-uniform Latin-hypercube scan of the whole box (deterministic\n+    # coverage of the global structure, randomized to avoid systematic bias).\n+    scan_n = max(16, int(iterations ** 0.5))\n+    grid = np.linspace(lo, hi, scan_n + 1)\n+    step = width / scan_n\n+    perm_x = np.random.permutation(scan_n)\n+    perm_y = np.random.permutation(scan_n)\n+    best_value = np.inf\n+    best_x = best_y = 0.0\n+    for i in range(scan_n):\n+        x = grid[perm_x[i]] + np.random.uniform(0.0, step)\n+        y = grid[perm_y[i]] + np.random.uniform(0.0, step)\n+        v = evaluate_function(x, y)\n+        if v < best_value:\n+            best_value, best_x, best_y = v, x, y\n+\n+    # Phase 2: annealed local refinement around the best point with occasional\n+    # restarts from other promising scan points (kept in a short list).\n+    remaining = max(0, iterations - scan_n)\n+    candidates = []\n+    for i in range(scan_n):\n+        x = grid[perm_x[i]] + np.random.uniform(0.0, step)\n+        y = grid[perm_y[i]] + np.random.uniform(0.0, step)\n+        candidates.append((evaluate_function(x, y), x, y))\n+    candidates.sort()\n+\n+    for i in range(remaining):\n+        progress = i / max(1, remaining - 1)\n+        if candidates and np.random.rand() < 0.1 * (1.0 - progress):\n+            # Restart from a secondary basin occasionally\n+            _, bx, by = candidates[min(len(candidates) - 1, 1 + int(np.random.rand() * 3))]\n+        else:\n+            bx, by = best_x, best_y\n+        sigma = 0.15 * width * (1.0 - progress) ** 2 + 1e-4\n+        x = min(max(bx + np.random.normal(0.0, sigma), lo), hi)\n+        y = min(max(by + np.random.normal(0.0, sigma), lo), hi)\n+        value = evaluate_function(x, y)\n+        if value < best_value:\n+            best_value, best_x, best_y = value, x, y\n+\n+    return best_x, best_y, best_value\n"}, "evaluator": {"candidate_cases": null, "parent_cases": null}, "generation": null, "metric": {"candidate": null, "delta": null, "direction": "maximize", "name": "score", "parent": null}, "proposer_harness_commit": null, "reason": "warning: recount: unexpected line: @@\n\nerror: corrupt patch at line 6\n", "schema_version": 1, "surface": null}
